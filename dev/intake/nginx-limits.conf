# Install in /etc/nginx/conf.d/nocturne-plugin-dev-limits.conf (http context).
limit_req_zone $binary_remote_addr zone=nocturne_plugin_dev_ip:1m rate=2r/s;
limit_req_zone $server_name zone=nocturne_plugin_dev_total:1m rate=10r/s;
